Commands for building Go library:

    go build -o ./bin/proxy_parser.so -buildmode=c-shared ./golang/proxy_parser/main.go
    go build -o ./bin/vk_group_checker.so -buildmode=c-shared ./golang/vk_group_checker/main.go

For running celery listener:

    celery -A sm_spy worker -l info
    celery -A sm_spy beat -l info